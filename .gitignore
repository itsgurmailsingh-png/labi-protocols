# Python
__pycache__/
*.py[cod]
.venv/
.env
*.egg-info/

# Pipeline credentials — NEVER commit
.env.local
secrets.json

# Raw downloads (large, regenerable)
data/raw/

# Binary media assets — hosted in the separate labi-protocols-media repo,
# referenced via CDN url in each protocol's media field, not tracked here.
data/media/

# OS
.DS_Store
Thumbs.db
