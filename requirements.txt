# Labi Protocol Database — Python dependencies
# Install with: pip install -r requirements.txt

requests>=2.31.0       # Protocols.io API calls
openai>=1.30.0         # GPT-4o-mini normalisation pass
datasketch>=1.6.5      # MinHash LSH for fast deduplication
python-docx>=1.1.0     # Extract text from .docx protocol attachments
pypdf>=4.0.0            # Extract text from .pdf protocol attachments
